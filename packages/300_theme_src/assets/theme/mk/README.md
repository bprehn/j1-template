# Mount Kato Theme Overlay

This directory is reserved for Mount Kato customizations.

The site still runs on the J1 template core (`template.name: j1`) to keep compatibility,
while MK-specific assets and overrides can be added here incrementally.
